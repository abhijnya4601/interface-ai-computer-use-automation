"""
Stretch goal: agent-facing capability interface.

Exposes the capabilities already sitting in /capabilities/ as a catalog an AI agent can
discover (catalog.py) and invoke by name with typed args (invoke.py) — see
scripts/demo_agent_capability_interface.py for a real Claude tool-use call doing exactly that,
and REPORT.md's "Stretch goal" section for the design reasoning.
"""
